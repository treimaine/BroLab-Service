export const ANIMATION_DURATION_MS = {
  FAST: 150,
  NORMAL: 300,
  SLOW: 500,
  VERY_SLOW: 1000,
} as const

export const ANIMATION_DELAY_MS = {
  SHORT: 100,
  MEDIUM: 200,
  LONG: 500,
} as const

export const STAGGER_DELAY_MS = 50

export const TRANSITION_DURATION_S = {
  FAST: 0.15,
  NORMAL: 0.3,
  SLOW: 0.5,
} as const
