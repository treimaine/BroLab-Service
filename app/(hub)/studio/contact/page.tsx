import { StudioContactClient } from './StudioContactClient'

export const metadata = {
  title: 'Contact Details | Studio | BroLab Entertainment',
  description: 'Manage the contact details shown on your storefront',
}

export default function StudioContactPage() {
  return <StudioContactClient />
}
